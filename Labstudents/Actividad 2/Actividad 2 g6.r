#Juan Manuel Saibene, Martín Balnco Desvars

#Ejercicio 1

# 1.  X<- c("bueno", "muy bueno", "excelente", "bueno")
# 2. new_x<-ordered(x, levels=c("excelente", "muy bueno", "bueno", "malo"))
# 3. x<-relevel(x,ref="excelente")
# 4. order_x<- factor(X, levels=c("excelente", "muy bueno", "bueno", "malo"), labels=c("e", "mb", "b", "m")) 

#Ejercicio 2

# C<- array(c(1:6, 10:15, 20:52), c(3, 2, 3))

# C[, , 3]

#Ejercicio 3

#1. df<- data.frame(género=c("M", "M", "M", "F", "F"), Colordepelo=c("negro", "negro","castaño","rubio", "pelirroja"), edad=c(20, 22, 24, 26, 19), pesoenkg= c(70, 56, 67, 75, 80), stringsAsFactors=FALSE)
#2. str(df)
#3. cbind(df, altura=c( 1.80, 1.75, 1.70, 1.65, 1.85))
#4. rbind(df, c("F","negro",28,53,1.65))
#5.  se transformaron todos en caracteres


